apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deom.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "deom.name" . }}
    helm.sh/chart: {{ include "deom.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "deom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
